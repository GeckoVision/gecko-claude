{
  "$comment": "Dev-only fallback: stdio MCP from a sibling gecko-mcpay-api clone. Use with `claude code --mcp-config .mcp.local.json` if you need to test unreleased changes locally.",
  "mcpServers": {
    "gecko": {
      "command": "uv",
      "args": ["--directory", "../../../gecko-mcpay-api", "run", "gecko-mcp"],
      "env": {}
    }
  }
}
